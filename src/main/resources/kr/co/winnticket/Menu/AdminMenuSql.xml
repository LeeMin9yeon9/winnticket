<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.winnticket.menu.admmenu.mapper.AdminMenuMapper">

    <!-- 관리자 메뉴 전체 조회 -->
    <select id="admMenuAllList" resultType="kr.co.winnticket.menu.admmenu.entity.AdminMenus">
        SELECT id,
               title,
               title_en AS titleEn,
               icon,
               page,
               display_order AS displayOrder,
               visible,
               created_at AS createdAt,
               updated_at AS updatedAt
        FROM admin_menus
        ORDER BY display_order ASC;
    </select>

    <!-- ID로 단건 조회 -->
    <select id="admMenuFindById" resultType="kr.co.winnticket.menu.admmenu.entity.AdminMenus">
        SELECT id,
               title,
               title_en AS titleEn,
               icon,
               page,
               display_order AS displayOrder,
               visible,
               created_at AS createdAt,
               updated_at AS updatedAt
        FROM admin_menus
        WHERE id = #{id, jdbcType=OTHER};
    </select>

    <!-- 관리자 메뉴 추가 -->
    <insert id="admMenuInsert">
        INSERT INTO admin_menus (
            id,
            title,
            title_en,
            icon,
            page,
            display_order,
            visible,
            created_at,
            updated_at
        ) VALUES (
                     #{id, jdbcType=OTHER},
                     #{title},
                     #{titleEn},
                     #{icon},
                     #{page},
                     #{displayOrder},
                     #{visible},
                     CURRENT_TIMESTAMP,
                     CURRENT_TIMESTAMP
                 );
    </insert>

    <!-- 관리자 메뉴 업데이트 -->
    <update id="admMenuUpdate">
        UPDATE admin_menus
        SET title        = #{title},
            title_en     = #{titleEn},
            icon         = #{icon},
            page         = #{page},
            display_order = #{displayOrder},
            visible      = #{visible},
            updated_at   = CURRENT_TIMESTAMP
        WHERE id = #{id ,jdbcType=OTHER};
    </update>

    <!-- 관리자 메뉴 삭제 -->
    <delete id="admMenuDelete">
        DELETE FROM admin_menus
        WHERE id = #{id, jdbcType=OTHER};
    </delete>

    <!-- 관리자 메뉴 노출 순서 변경 -->
    <update id="admMenuUpdateOrder">
        UPDATE admin_menus
        SET display_order = #{order},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id, jdbcType=OTHER};
    </update>

    <!-- 관리자 메뉴 활성/비활성 변경 -->
    <update id="admMenuUpdateVisible">
        UPDATE admin_menus
        SET visible = #{visible},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id,jdbcType=OTHER};
    </update>

    <!-- 신규 메뉴 생성 시 자동 순서 부여 -->
    <select id="admMenuGetMaxOrder" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(display_order), 0)
        FROM admin_menus;
    </select>

    <!--존재페이지 여부-->
    <select id="admMenuCountByPage" resultType="int">
        SELECT COUNT(*)
        FROM admin_menus
        WHERE page = #{page}
    </select>

    <select id="admMenuSearch"
             parameterType="kr.co.winnticket.menu.admmenu.dto.AdminMenuSearchDto"
             resultType="kr.co.winnticket.menu.admmenu.entity.AdminMenus">
    SELECT *
    FROM admin_menus
    <where>
        <if test="title != null and title != ''">
            AND title ILIKE CONCAT('%', #{title}, '%')
        </if>
        <if test="page != null and page != ''">
            AND page ILIKE CONCAT('%', #{page}, '%')
        </if>
    </where>
</select>

</mapper>
