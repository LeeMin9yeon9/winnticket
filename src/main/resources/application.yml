server:
  port: 8080

spring:
  profiles:
    active: local   # 기본은 local

mybatis:
  mapper-locations:
    - classpath:/kr/co/winnticket/community/*.xml
    - classpath:/kr/co/winnticket/Menu/*.xml
  type-aliases-package: kr.co.winnticket
  config-location: classpath:mybatis-config.xml
  configuration:
    map-underscore-to-camel-case: true
    type-handlers-package: kr.co.winnticket.common.typehandler

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /api-docs

  default-consumes-media-type: application/json
  default-produces-media-type: application/json

  override-with-generic-response: false

logging:
  level:
    kr.co.winnticket: DEBUG
# 테